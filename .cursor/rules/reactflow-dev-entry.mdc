---
description: flowchart-web 表駆動コード編集時は REACTFLOW_RULES・creating-reactflow-yk・ROUTER を入口とする
globs: "**/flowchart-web-*/**/*.{ts,tsx}"
alwaysApply: false
---

# React Flow / 表駆動フロー — エージェント入口

**正本:** 本ファイル。`yk-memo` · `yk-tool` の同名 `.mdc` は同期コピー（更新時は本ファイルを先に直す）。

`flowchart-web-reactflow` · `flowchart-web-mermaid` 配下の TS/TSX を編集するとき:

1. `c:/yk-skill/rule/35_reactflow/REACTFLOW_RULES.md` — L1 SSOT
2. `c:/yk-skill/.claude/skills/creating-reactflow-yk/SKILL.md` — 手順
3. `.../creating-reactflow-yk/references/ROUTER.md` — tier / tag
4. **Ref Plan**（Write/StrReplace 前）→ ROUTER §7

- Next シェル（`app/` のみ）→ `creating-nextjs-yk` · `NEXTJS_RULES` §5（RSC）· §6（flowchart）
- `components/flowchart` の React 一般 → `creating-react-yk` · `REACT_RULES`
- shadcn 導入・`components/ui/**` · `components.json` → **`shadcn-dev-entry.mdc`** · `creating-shadcn-yk` · `SHADCN_UI_RULES` §13 · ROUTER tag `init-radix`（未導入時 Full）
- **UI 整理（機能削除）** → `flowchart-web-reactflow/.cursor/rules/ui-simplification-yk.mdc` · `REACTFLOW_RULES` §5.5
- **実用版 UX（3ペイン・ツールバー・警告）** → `flowchart-web-reactflow/.cursor/rules/flowchart-practical-ux-yk.mdc` · `REACTFLOW_RULES` §5.6
- **ローカル確認（localhost · 外部ブラウザ）** → `flowchart-web-reactflow/.cursor/rules/local-dev-browser-yk.mdc` · `docs/LOCAL_DEV.md`
- `.mmd` → `creating-mermaid-yk`（本入口非使用）
- 完了報告: `REACTFLOW_RULES.md` §6〜§7 + **読んだ refs**
