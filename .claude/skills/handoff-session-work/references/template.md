# セッション引き継ぎ — テンプレート SSOT

終了時は本ファイルの構造どおりに **新規** Markdown を Write する。見出しは削除・リネームしない。空欄・`TBD` 禁止。

**書き先:** `c:/yk-memo/handoffs/{project}/{YYYY-MM-DD}_{slug}.md`（命名 → [routing.md](routing.md)）

---

## ファイル先頭（コピーして埋める）

```markdown
# {プロジェクト名} — セッション引き継ぎ（{YYYY-MM-DD}）

> **次回 AI 向け:** **やること**は §「次回の最初の 1 件」。本ファイルは経緯・完了記録用。

| 項目 | 値 |
|------|-----|
| **セッション日** | {YYYY-MM-DD} |
| **プロジェクト slug** | {handoffs/{project}/ のフォルダ名} |
| **状態** | {一行サマリー · 例: 進行中 / 本セッション分は引き継ぎ終了済} |
| **次の 1 手** | §「次回の最初の 1 件」のみ（HANDOFF ロードマップ全体ではない） |
| **推奨 workspace** | {例: yk-memo + yk-skill + yk-tool} |
| **commit** | 終了時 Phase C で commit+push（結果を §2 に記録） |

---

## 1. 本セッションで完了したこと

### 1-1. 決定事項（理由付き）

| 論点 | 決定 | 理由（一行） |
|------|------|--------------|
| | | |

### 1-2. 却下した案

| 案 | 却下理由 |
|----|----------|
| | |

### 1-3. 実施内容・触ったパス

- {箇条書き}
- **資料整理（実施した場合）:** 削除 · `archive/` 移動したファイル名
- **触った主なパス（リポ別）**
  - `{repo}`: `{path}` …

---

## 2. Git 状態（セッション終了時点）

Phase B で各触った Git ルートの `git status` を記載。**Phase C 後**に同表を更新（commit hash · push 済）。

| リポ（ルート） | 状態（要約） |
|----------------|--------------|
| | 例: クリーン · `<hash>` · push 済 / スキップ（理由 1 行） |

**Phase C スキップ時:** {secrets · rev 保護 · 変更なし 等 — 1 行}

---

## 3. 未実施（意図的に残したこと）

- {箇条書き — 次セッション用の入力}

---

## 4. 次回の最初の 1 件

> **再開時:** 本 § だけ実行。プロジェクト HANDOFF の §6 等ロードマップ全体には広げない。

### 完了の定義（この 1 件）

- {チェックリスト — 測定可能な条件}

### やる / やらない

| やる | やらない |
|------|----------|
| | |

### 依頼文（コピペ用）

```text
@c:/yk-memo/handoffs/{project}/{このファイル名}.md
@c:/yk-memo/handoffs/{project}/HANDOFF.md
続きから。一つずつ順番に進めてください。
```

---

## 5. その後の順序（ロードマップ · 参考）

| # | タスク | 状態 |
|---|--------|------|
| 1 | | 完了 / 次 / 未着手 |

**完了したら:** ユーザーに確認 → 次の # に進むか聞く。

---

## 6. 既知の注意点

| 項目 | メモ |
|------|------|
| | |

---

## 7. 副次タスク（別トラック）

- {本スキル・本プロジェクト外のメモ — リンクのみ}

---

**最終更新:** {YYYY-MM-DD}（セッション終了）
```

---

## 記入ルール

| ルール | 内容 |
|--------|------|
| SSOT | `RULE_INDEX` · 各 `*_RULES.md` · プロジェクト HANDOFF は **パスリンクのみ** |
| 禁止 | rule 全文 · 巨大 HANDOFF の複製 · 秘密情報・PII の貼付 |
| 必須 | §4「完了の定義」· §2 Git 表 · §4 依頼文 |
| 次回 | §4 の 1 件だけ実行（「一つずつ」指定時は 1 タスクで停止） |
| ライフサイクル | 用語・終了ゲート → [routing.md §引き継ぎ終了](routing.md) |